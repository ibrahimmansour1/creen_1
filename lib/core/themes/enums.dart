enum ReportType {
  user,
  post,
  live,
  product,
  chat,
  chatMessage,
  comment
}

enum OrderStatus{
  accepted,
  pending,
  delivered,
  canceled,
}

enum Account{
  email,
  apple,
  google,
  facebook,
  x,
  weChat,
}

enum LiveType{
  video,
  sound,
  audio,
  writing,
}

enum LivePermission{
  public,
  followers,
  subscripers,
  admins,
  no_one,
}
